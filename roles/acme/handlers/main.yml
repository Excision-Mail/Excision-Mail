- name: restart httpd
  service:
          name: httpd
          enabled: yes
          state: restarted

- name: create acme certs
  shell: acme-client -f /etc/ansimail/acme-client.conf {{ hostname }}
  ignore_errors: yes


